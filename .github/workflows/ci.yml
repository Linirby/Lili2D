name: Lili2D CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: ${{ matrix.os }} - ${{ matrix.compiler }} (C++20)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        compiler: [gcc, clang]
        include:
          - compiler: gcc
            cc: gcc
            cxx: g++
          - compiler: clang
            cc: clang
            cxx: clang++

    env:
      DEPS_DIR: /home/runner/deps
      CMAKE_PREFIX_PATH: /home/runner/deps
      PKG_CONFIG_PATH: /home/runner/deps/lib/pkgconfig:/home/runner/deps/lib64/pkgconfig
      LD_LIBRARY_PATH: /home/runner/deps/lib:/home/runner/deps/lib64
      PATH: /home/runner/deps/bin:/usr/local/bin:/usr/bin:/bin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up environment
        run: |
          echo "CC=${{ matrix.cc }}" >> $GITHUB_ENV
          echo "CXX=${{ matrix.cxx }}" >> $GITHUB_ENV

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            ninja-build \
            pkg-config \
            libasound2-dev \
            libpulse-dev \
            libx11-dev \
            libxext-dev \
            libxrandr-dev \
            libxcursor-dev \
            libxfixes-dev \
            libxi-dev \
            libxss-dev \
            libxtst-dev \
            libxkbcommon-dev \
            libgl1-mesa-dev \
            libgles2-mesa-dev \
            libegl1-mesa-dev \
            libdbus-1-dev \
            libudev-dev \
            libwayland-dev \
            libdecor-0-dev \
            libpipewire-0.3-dev \
            libjpeg-dev \
            libpng-dev \
            libwebp-dev \
            libtiff-dev

      - name: Cache SDL3 dependencies
        id: cache-sdl3
        uses: actions/cache@v4
        with:
          path: /home/runner/deps
          key: sdl3-deps-v3-${{ runner.os }}-${{ matrix.compiler }}
          restore-keys: |
            sdl3-deps-v3-${{ runner.os }}-

      - name: Build & Install SDL3 from source
        if: steps.cache-sdl3.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 https://github.com/libsdl-org/SDL.git /tmp/SDL
          cmake -B /tmp/SDL/build -S /tmp/SDL -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$DEPS_DIR -DSDL_TESTS=OFF
          cmake --build /tmp/SDL/build -j$(nproc)
          cmake --install /tmp/SDL/build

      - name: Build & Install SDL3_image from source
        if: steps.cache-sdl3.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 https://github.com/libsdl-org/SDL_image.git /tmp/SDL_image
          cmake -B /tmp/SDL_image/build -S /tmp/SDL_image -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$DEPS_DIR -DSDLIMAGE_TESTS=OFF
          cmake --build /tmp/SDL_image/build -j$(nproc)
          cmake --install /tmp/SDL_image/build

      - name: Build & Install SDL3_shadercross from source
        if: steps.cache-sdl3.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 https://github.com/libsdl-org/SDL_shadercross.git /tmp/SDL_shadercross
          cd /tmp/SDL_shadercross
          ./external/download.sh
          cmake -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$DEPS_DIR -DSDLSHADERCROSS_STATIC=OFF -DSDLSHADERCROSS_VENDORED=ON
          cmake --build build -j$(nproc)
          cmake --install build

      - name: Configure CMake
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH=$DEPS_DIR \
            -DLILI2D_BUILD_TESTS=ON

      - name: Build Lili2D and Test Suite
        run: |
          cmake --build build --target lili2d_tests -j$(nproc)

      - name: Run Test Suite (CTest)
        run: |
          ctest --test-dir build --output-on-failure --schedule-random

      - name: Install Lili2D locally
        run: |
          cmake --install build --prefix $DEPS_DIR

      - name: Test Example Applications Build
        run: |
          for example in examples/*/; do
            if [ -f "$example/CMakeLists.txt" ]; then
              echo "=== Building $example ==="
              cmake -B "$example/build" -S "$example" -G Ninja \
                -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_PREFIX_PATH=$DEPS_DIR
              cmake --build "$example/build" -j$(nproc)
            fi
          done
